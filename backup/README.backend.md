Local dev backend scaffold (minimal)

Run in dev mode:

```bash
npm install
npm run dev
```

Endpoints:
- POST /api/npc
- GET /api/npc
- GET /api/npc/:id

WebSocket: ws on same server (wss) - receives broadcasts: npc_created
