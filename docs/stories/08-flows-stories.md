```markdown
# 08 - 关键交互流程：用户故事

1. NPC 创建流程
   - 作为玩家，我通过前端表单提交创建请求，后端验证 player_id 并广播创建事件。
   - 验收标准：前端提交 `POST /npc`，后端返回 201 并广播 `npc_created`；前端更新列表。

2. 仿真步与广播
   - 作为系统，我要按节奏调用 LLM Adapter 为每个 NPC 生成行动并广播 `state_update`。
   - 验收标准：系统在配置间隔（默认 90s）触发决策；`state_update` 包含行为摘要与状态变更。

```
